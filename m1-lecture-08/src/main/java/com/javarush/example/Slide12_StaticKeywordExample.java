package com.javarush.example;

/**
 * Пример из слайда 12: "Ключевое слово static"
 *
 * Ключевое слово static используется для объявления статических методов и полей.
 * Они принадлежат классу, а не объекту — вызываются через ИмяКласса.имяМетода().
 */
public class Slide12_StaticKeywordExample {

    // 👇 Статическое поле — общее для всех объектов
    public static int counter = 0;

    // 👇 Нестатическое поле — уникальное для каждого объекта
    private String name;

    // 👇 Статический метод — не требует объекта
    public static void incrementCounter() {
        counter++;
        System.out.println("Статический счётчик: " + counter);
        // ❌ Нельзя вызвать: System.out.println(name); — name не static!
    }

    // 👇 Нестатический метод — требует объекта
    public void setName(String name) {
        this.name = name;
        System.out.println("Установлено имя: " + this.name);
    }

    // 👇 Статический метод, который может вызывать нестатический — только через объект
    public static void callNonStaticMethod() {
        Slide12_StaticKeywordExample obj = new Slide12_StaticKeywordExample();
        obj.setName("Анна"); // ✅ Можно, потому что мы создали объект
    }

    public static void main(String[] args) {
        System.out.println("=== Ключевое слово static ===");

        // ✅ Статический метод вызывается через имя класса
        Slide12_StaticKeywordExample.incrementCounter();
        Slide12_StaticKeywordExample.incrementCounter();

        // ✅ Статическое поле — общее для всех
        System.out.println("Значение counter из main(): " + Slide12_StaticKeywordExample.counter);

        // 👇 Создаём объект — чтобы вызвать нестатический метод
        Slide12_StaticKeywordExample obj = new Slide12_StaticKeywordExample();
        obj.setName("Иван");

        // ✅ Статический метод, который создаёт объект и вызывает нестатический
        Slide12_StaticKeywordExample.callNonStaticMethod();

        System.out.println("\n✅ Статические методы удобны для утилит, констант и общих операций.");
    }
}
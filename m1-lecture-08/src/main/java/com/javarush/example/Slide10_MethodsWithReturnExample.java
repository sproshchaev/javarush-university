package com.javarush.example;


/**
 * Пример из слайда 10: "Функции с результатом, void"
 *
 * Методы могут возвращать значение (тип != void) или ничего (void).
 * Для возврата используется ключевое слово return.
 */
public class Slide10_MethodsWithReturnExample {

    public static void main(String[] args) {
        System.out.println("=== Методы с возвращаемым значением и void ===");

        // 👇 Методы с возвращаемым значением
        int sum = add(5, 3);
        System.out.println("Сумма: " + sum);

        String greeting = createGreeting("Анна");
        System.out.println(greeting);

        boolean isEven = isNumberEven(8);
        System.out.println("Число 8 чётное? " + isEven);

        // 👇 Методы с void — ничего не возвращают
        printSeparator();
        printResult("Результат выполнения", 100);
        printSeparator();

        // 👇 Демонстрация: return в void-методе
        earlyExitIfNegative(-5); // вызов с отрицательным числом — выйдет сразу
        System.out.println("Эта строка не выведется, если число было отрицательным.");
    }

    /**
     * Метод add — складывает два числа и возвращает результат.
     *
     * @param a первое число
     * @param b второе число
     * @return сумма a и b
     */
    public static int add(int a, int b) {
        return a + b;
    }

    /**
     * Метод createGreeting — создаёт приветствие.
     *
     * @param name имя пользователя
     * @return строка с приветствием
     */
    public static String createGreeting(String name) {
        return "Здравствуйте, " + name + "!";
    }

    /**
     * Метод isNumberEven — проверяет, чётное ли число.
     *
     * @param number число для проверки
     * @return true, если чётное, false — если нечётное
     */
    public static boolean isNumberEven(int number) {
        return number % 2 == 0;
    }

    /**
     * Метод printSeparator — выводит разделительную линию.
     * Не возвращает ничего — тип void.
     */
    public static void printSeparator() {
        System.out.println("====================");
    }

    /**
     * Метод printResult — выводит сообщение и значение.
     * Тип void — ничего не возвращает.
     *
     * @param message описание результата
     * @param value   значение результата
     */
    public static void printResult(String message, int value) {
        System.out.println(message + ": " + value);
    }

    /**
     * Метод earlyExitIfNegative — досрочно завершает работу, если число отрицательное.
     * Использует return в void-методе.
     *
     * @param number число для проверки
     */
    public static void earlyExitIfNegative(int number) {
        if (number < 0) {
            System.out.println("⚠️ Число отрицательное — выход из метода.");
            return; // 👇 досрочный выход — остальной код не выполнится
        }
        System.out.println("✅ Число положительное или ноль — продолжаем.");
    }
}
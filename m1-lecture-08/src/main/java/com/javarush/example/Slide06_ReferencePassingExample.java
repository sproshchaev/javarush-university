package com.javarush.example;

/**
 * Пример из слайда 6: "Передача ссылок в методы"
 *
 * В Java массивы и объекты передаются по ссылке — точнее, передаётся копия ссылки.
 * Изменения в методе влияют на исходный объект.
 */
public class Slide06_ReferencePassingExample {

    public static void main(String[] args) {
        System.out.println("=== Передача ссылок в методы ===");

        int[] numbers = {10, 20, 30};

        System.out.print("Массив до вызова метода: ");
        printArray(numbers);

        // 👇 Передаём массив в метод — изменяем его
        modifyArray(numbers);

        System.out.print("Массив после вызова метода: ");
        printArray(numbers); // ⚠️ Изменения остались!

        System.out.println("\n--- Демонстрация присваивания ссылок ---");

        int[] copyRef = numbers; // Обе переменные ссылаются на один массив
        copyRef[0] = 999;

        System.out.print("numbers[0] = " + numbers[0]); // Выведет 999!
        System.out.println(" — потому что numbers и copyRef ссылаются на один объект!");
    }

    /**
     * Метод, изменяющий переданный массив.
     * Изменения видны и за пределами метода.
     *
     * @param arr — массив, который нужно изменить
     */
    public static void modifyArray(int[] arr) {
        for (int i = 0; i < arr.length; i++) {
            arr[i] *= 2; // Удваиваем каждый элемент
        }
        System.out.println("✅ Массив был изменён внутри метода.");
    }

    /**
     * Вспомогательный метод для вывода массива.
     *
     * @param arr — массив для вывода
     */
    public static void printArray(int[] arr) {
        for (int num : arr) {
            System.out.print(num + " ");
        }
        System.out.println();
    }
}
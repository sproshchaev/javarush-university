package com.javarush.example;

/**
 * Пример из слайда 13: "Локальные переменные"
 *
 * Все переменные, объявленные внутри методов или блоков, называются локальными.
 * Они существуют только в пределах того блока, где объявлены.
 */
public class Slide13_LocalVariablesExample {

    public static void main(String[] args) {
        System.out.println("=== Локальные переменные ===");

        int a = 5;
        System.out.println("Переменная a (в main): " + a);

        if (a < 10) {
            int b = 10;
            System.out.println("Переменная b (в if): " + b);

            while (true) {
                int x = a + b;
                System.out.println("Переменная x (в while): " + x);

                // 👇 Можно использовать a и b — они видны в этом блоке
                System.out.println("a + b = " + (a + b));

                break; // чтобы цикл не был бесконечным
            }

            // ✅ b всё ещё доступна — мы в том же блоке, что и if
            System.out.println("b после while: " + b);
        }

        // ❌ ОШИБКА КОМПИЛЯЦИИ: x не существует за пределами while
        // System.out.println("x за пределами while: " + x);

        // ✅ a доступна — она объявлена в main()
        System.out.println("a после if: " + a);

        // ❌ ОШИБКА КОМПИЛЯЦИИ: повторное объявление b в том же блоке
        // int b = 20; // Нельзя!

        System.out.println("\n--- Демонстрация области видимости ---");
        demonstrateScope();
    }

    /**
     * Метод для демонстрации области видимости локальных переменных.
     */
    public static void demonstrateScope() {
        int outer = 100;

        if (outer > 50) {
            int inner = 200;
            System.out.println("inner (в if): " + inner);
            System.out.println("outer (в if): " + outer);
        }

        // ❌ ОШИБКА: inner не существует за пределами if
        // System.out.println("inner за пределами if: " + inner);

        System.out.println("outer за пределами if: " + outer);
    }
}

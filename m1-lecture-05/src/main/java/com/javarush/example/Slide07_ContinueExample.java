package com.javarush.example;

public class Slide07_ContinueExample {
    public static void main(String[] args) {
        System.out.println("=== Пример команды continue ===\n");

        // ✅ РАБОЧИЙ ПРИМЕР: вывод чисел от 1 до 20, кроме делящихся на 7
        System.out.println("✅ Рабочий пример: пропускаем числа, делящиеся на 7");
        for (int i = 1; i <= 20; i++) {
            if (i % 7 == 0) {
                continue; // пропускаем эту итерацию
            }
            System.out.print(i + " ");
        }
        System.out.println("\n\n---\n");

        // ❌ ОШИБОЧНЫЙ ПРИМЕР (как в слайде): приводит к бесконечному циклу!
        System.out.println("❌ Ошибочный пример (как в слайде): i застревает на 7");
        int i = 1;
        while (i <= 20) {
            if (i % 7 == 0) {
                continue; // 🚫 здесь i НЕ увеличивается → бесконечный цикл!
            }
            System.out.print(i + " ");
            i++; // 🔁 эта строка НЕ выполнится, если сработал continue
        }
        // ⚠️ Этот код НЕ ЗАВЕРШИТСЯ — он зациклится на i=7

        System.out.println("\n\n⚠️ Внимание: ошибочный пример выше вызовет бесконечный цикл!");
        System.out.println("Не запускайте его без исправления!");

        // ✅ ИСПРАВЛЕННЫЙ ОШИБОЧНЫЙ ПРИМЕР:
        System.out.println("\n✅ Исправленный вариант ошибочного примера:");
        i = 1;
        while (i <= 20) {
            if (i % 7 == 0) {
                i++; // ✅ сначала увеличиваем i, потом continue
                continue;
            }
            System.out.print(i + " ");
            i++;
        }
        System.out.println("\n\n✅ Теперь всё работает корректно.");
    }
}

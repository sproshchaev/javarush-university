## 🎯 **ОСНОВНЫЕ ПАМЯТКИ ПО GIT**

---

## 🔄 **ОБНОВЛЕНИЕ ИЗ РЕПОЗИТОРИЯ sproshchaev**

### **Первоначальная настройка (делается один раз)**

```bash
# Клонируем свой форк
git clone https://github.com/ВАШ_АККАУНТ/javarush-university.git
cd javarush-university

# Добавляем оригинальный репозиторий как upstream
git remote add upstream https://github.com/sproshchaev/javarush-university.git

# Проверяем что всё добавилось
git remote -v
```
**Должно показать:**
```
origin    https://github.com/ВАШ_АККАУНТ/javarush-university.git (fetch)
origin    https://github.com/ВАШ_АККАУНТ/javarush-university.git (push)
upstream  https://github.com/sproshchaev/javarush-university.git (fetch)
upstream  https://github.com/sproshchaev/javarush-university.git (push)
```

---

### **Получение обновлений из sproshchaev**

#### Обновление ветки
```bash
# Переключаемся на main
git checkout main

# Получаем изменения из upstream
git fetch upstream

# Вливаем изменения в свою ветку main
git merge upstream/main

# Создаем локальную ветку base и связываем с upstream
git checkout -b base upstream/base

# Отправляем в свой форк
git push -u origin base
```

---

### **Как получить изменения из sproshchaev/javarush-university в свою длкальную ветку base и запушить в свой репозиторий **

```bash
# 1. Получить свежие изменения из sproshchaev/javarush-university
git fetch upstream

# 2. Перейти в нужную ветку
git checkout base

# 3. Влить изменения
git merge upstream/base

# 4. Обновить свой форк на GitHub
git push origin base
```

---

### **Проверка результата**

```bash
# Посмотреть все ветки
git branch -a

# Посмотреть историю коммитов
git log --oneline -5

# Проверить статус
git status

# Посмотреть связи веток
git remote show upstream
```

---

### **Решение частых проблем**

#### **Ошибка доступа при push**
```bash
# Создаем Personal Access Token на GitHub
# Затем обновляем URL:
git remote set-url origin https://TOKEN@github.com/ВАШ_АККАУНТ/javarush-university.git
```

#### **Конфликты при слиянии**
1. Редактируем файлы с конфликтами
2. Убираем маркеры `<<<<<<<`, `=======`, `>>>>>>>`
3. Сохраняем нужную версию кода
4. Выполняем:
```bash
git add .
git commit
git push origin main
```

---

### **Когда выполнять обновление?**

- ✅ **Когда появляется запись вебинара на YouTube** - значит в репозитории есть новые материалы
- ✅ **Перед началом работы** над новым заданием
- ✅ **Если коллеги сообщили о новых изменениях**
- ✅ **Раз в неделю** для регулярной синхронизации

---

### **Формула успеха 🎯**

```
git fetch upstream + git merge upstream/main + git push origin main
```

**Выполняйте эту последовательность, и ваш форк всегда будет актуальным!**

---

## 📁 **1. БАЗОВЫЕ КОМАНДЫ**

### **Настройка Git**
```bash
git config --global user.name "Ваше Имя"
git config --global user.email "your.email@example.com"
git config --list
```

### **Инициализация и статус**
```bash
git init                        # Создать новый репозиторий
git status                      # Показать состояние файлов
git log --oneline              # Краткая история коммитов
git log --oneline --graph --all # Визуализация истории с ветками
```

### **Работа с файлами**
```bash
git add .                       # Добавить все файлы в staging
git add README.md              # Добавить конкретный файл
git commit -m "сообщение"      # Создать коммит
git push                       # Отправить изменения на сервер
```

---

## 🌿 **2. РАБОТА С ВЕТКАМИ**

### **Создание и переключение**
```bash
git branch                      # Показать все ветки
git branch feature/new-feature  # Создать новую ветку
git checkout feature/new-feature # Переключиться на ветку
git checkout -b feature/new-feature # Создать и переключиться
git switch feature/new-feature  # Современный синтаксис переключения
```

### **Слияние веток**
```bash
git checkout main              # Перейти в основную ветку
git merge feature/new-feature  # Влить изменения из ветки
```

---

## 🔄 **3. СИНХРОНИЗАЦИЯ С УДАЛЕННЫМ РЕПОЗИТОРИЕМ**

### **Клонирование и remote**
```bash
git clone https://github.com/username/repository.git
git remote -v                   # Показать удаленные репозитории
git remote add upstream https://github.com/original/repo.git
```

### **Получение обновлений**
```bash
git pull                       # Скачать и объединить изменения
git fetch upstream            # Получить изменения из оригинала
git merge upstream/main       # Влить изменения в локальную ветку
```

### **Форк и синхронизация**
```bash
# После создания форка на GitHub:
git clone https://github.com/YOUR-USERNAME/repo.git
git remote add upstream https://github.com/ORIGINAL-OWNER/repo.git
git fetch upstream
git merge upstream/main
git push origin main
```

---

## ⚡ **4. РАБОТА С ИЗМЕНЕНИЯМИ**

### **Просмотр изменений**
```bash
git diff                       # Показать неподготовленные изменения
git diff --staged             # Показать подготовленные изменения
git status                    # Текущее состояние
```

### **Отмена изменений**
```bash
git checkout -- file.txt       # Откатить изменения в файле
git restore file.txt          # Современный синтаксис отката
git reset --hard HEAD         # Полный откат к последнему коммиту
```

---

## 🚨 **5. РАЗРЕШЕНИЕ КОНФЛИКТОВ**

### **При конфликте слияния:**
1. Открыть файл с конфликтами
2. Найти блоки между `<<<<<<<`, `=======`, `>>>>>>>`
3. Выбрать нужную версию кода
4. Удалить маркеры конфликта
5. Сохранить файл
6. Выполнить:
```bash
git add .
git commit
```

---

## 🔐 **6. НАСТРОЙКА ДОСТУПА**

### **Personal Access Token**
1. GitHub → Settings → Developer settings → Personal access tokens
2. Generate new token → Generate new token (classic)
3. Scopes: `repo`, `workflow`, `write:packages`
4. Обновить remote URL:
```bash
git remote set-url origin https://TOKEN@github.com/username/repo.git
```

---

## 📝 **7. ТИПЫ КОММИТОВ (Conventional Commits)**

```
feat:     Новая функциональность
fix:      Исправление ошибки
docs:     Изменения в документации
style:    Форматирование кода
refactor: Рефакторинг кода
test:     Добавление тестов
chore:    Обслуживание проекта
```

**Примеры:**
```bash
git commit -m "feat: add user authentication"
git commit -m "fix: resolve null pointer exception"
git commit -m "docs: update API documentation"
```

---

## 📋 **8. .GITIGNORE - ЧТО ИГНОРИРОВАТЬ**

### **Базовые правила:**
```gitignore
# IDE файлы
.idea/
*.iml

# Временные файлы
*.log
*.tmp

# Зависимости
node_modules/
venv/
target/

# Системные файлы
.DS_Store
Thumbs.db
```

**Сайт для генерации:** https://www.toptal.com/developers/gitignore

---

## 🛠 **9. HOTKEYS INTELLIJ IDEA**

- `Ctrl + K` - Коммит изменений
- `Ctrl + Shift + K` - Пуш изменений
- `Ctrl + T` - Обновить проект (Pull)
- `Alt + Ctrl + Z` - Откат изменений в файле
- `Ctrl + D` - Показать diff изменений

---

## ⚠️ **10. ВАЖНЫЕ ПРАВИЛА**

### **Золотые правила:**
1. **Никогда не делайте reset --hard после push**
2. **Всегда делайте pull перед началом работы**
3. **Коммитьте часто, пушите реже**
4. **Одна ветка - одна задача**
5. **Пишите понятные сообщения коммитов**

### **После push - только новые коммиты!**
```bash
# ПРАВИЛЬНО:
git commit -m "fix: additional corrections"
git push

# НЕПРАВИЛЬНО:
git commit --amend
git push --force
```

---

## 🔗 **11. ПОЛЕЗНЫЕ РЕСУРСЫ**

- **Pro Git книга**: https://git-scm.com/book/ru/v2
- **GitHub Learning**: https://github.com/ru
- **Интерактивный тренажер**: https://learngitbranching.js.org
- **Официальная документация**: https://git-scm.com/doc

